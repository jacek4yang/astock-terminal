# 生产重构记录（2026-08-22）

基线：`aff1a70e61500d3f657617c1f3830ebc4e651e48`。

## 已完成

- 修复 Agent 历史 object/string 契约导致的黑屏，增加根、路由和 Agent Error Boundary。
- 分离 conversation/run，使用预绑定 Channel、事件序号、持久会话和应用重启恢复。
- 修复 MiniMax 2013 中断工具链，保留 provider reasoning content，更新 M3/M2.7 fallback，接入 Token Plan 明细。
- Security Master、字段来源、缺失语义、TDX 名称合并、全市场身份搜索和 v6 存储迁移。
- TDX 五档、完整沪深列表、行情/K线合并及 300308/000001 身份回归。
- 指数异常数据过滤、周末行/孤立尖峰检测和涨跌幅重算。
- 动态图谱公司节点与有来源的行业关系，静态图谱缺失不再返回 raw node-not-found。
- 专业全市场分页/筛选/排序工作台、持久可调整导航和 Agent 抽屉。
- 后台扫描与回测、真实取消/状态恢复；Agent 进度、工具耗时/来源/失败展示。
- 有边界的策略迭代和结构化人工交易计划。

## 仍属客观限制

- TDX 当前产品化能力覆盖列表、快照/五档、K线和分钟；逐笔、财务、除权与板块协议仍未全部接入 UI。
- 免费来源无法保证全量供应链边、公告和严格 point-in-time 财务覆盖。
- 回测后台状态在同一应用进程内保留；Agent 任务额外持久化到磁盘并支持进程重启恢复。
- Windows 缩放已按响应式/最小宽度设计并做主尺寸验证，但未取得全部列出的物理显示器组合。

不得将以上限制描述为已完成能力。
