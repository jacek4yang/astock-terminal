[package]
name = "astock-backtest"
description = "A-share backtesting engine: event-driven + vectorized, trading rules aware, anti-overfitting harness"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
[dependencies]
astock-core = { version = "6.0.0", path = "../core" }
astock-trading-rules = { version = "6.0.0", path = "../trading-rules" }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
chrono.workspace = true
rand.workspace = true

[dev-dependencies]
proptest.workspace = true
