# Keep the shared Rust workspace platform-neutral. Windows build scripts set
# CARGO_TARGET_DIR beneath ASTOCK_BUILD_ROOT; other platforms use Cargo's
# native default unless the caller explicitly overrides it.
